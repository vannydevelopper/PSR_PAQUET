export const loadingOrdersSelector = ({ orders }) => orders.loading
export const ordersSelector = ({ orders }) => orders.orders
export const successTitleSelector = ({ orders }) => orders.successTitle
export const successContentSelector = ({ orders }) => orders.successContent
export const showSuccesModalSelector = ({ orders }) => orders.showSuccesModal